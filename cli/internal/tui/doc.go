// Package tui is the interactive Bubble Tea front end for restforge -- the
// terminal sibling of the Flutter app's UI, driven by internal/session.
//
// See placeholder.go for the current state: a not-yet-implemented shim that
// lets the binary's mode-by-invocation dispatch be wired and tested before
// the real TUI's task arrives.
package tui
