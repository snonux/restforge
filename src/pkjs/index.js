/* RESTForge companion stub.
 *
 * RESTForge is entirely on-watch: this file exists only because the build
 * (see wscript, js_entry_file) always bundles a PebbleKit JS entry point. */

Pebble.addEventListener('ready', function() {
  console.log('RESTForge companion ready');
});